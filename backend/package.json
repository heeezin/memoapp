{
  "name": "backend",
  "version": "1.0.0",
  "main": "api-server.js",
  "scripts": {
    "start": "node api-server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "cd backend && npm install && npm run build && mv dist ../src"
  },
  "keywords": [
    "example",
    "heroku"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "express": "^4.18.2",
    "mariadb": "^3.0.2"
  },
  "engines": {
    "node": "16.15.0",
    "npm": "8.5.5"
  },
  "description": ""
}
